apiVersion: pool.w-h-a.io/v1alpha1
kind: ReserveSet
metadata:
  labels:
    app.kubernetes.io/name: reserve
    app.kubernetes.io/managed-by: kustomize
  name: reserveset-sample
spec:
  # TODO(user): Add fields here
